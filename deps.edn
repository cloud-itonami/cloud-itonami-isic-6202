{:paths ["src"]
 :deps {io.github.kotoba-lang/langgraph
        {:local/root "../../kotoba-lang/langgraph"}
        io.github.kotoba-lang/crm
        {:local/root "../../kotoba-lang/crm"}
        ;; HTTP service layer (src/svcdesk/http.clj, JVM-only compat glue —
        ;; see its ns docstring for why this is not a .cljc violation).
        ;; Same versions as cloud-itonami-isic-5820's src/crm/http.clj.
        http-kit/http-kit        {:mvn/version "2.8.1"}
        ring/ring-core           {:mvn/version "1.15.3"}
        org.clojure/data.json    {:mvn/version "2.5.2"}}
 :aliases
 {:dev {:override-deps
        {io.github.kotoba-lang/langchain
         {:local/root "../../kotoba-lang/langchain"}}}
  :run {:main-opts ["-m" "svcdesk.sim"]}
  ;; clojure -M:serve → starts the real HTTP service (src/svcdesk/http.clj).
  ;; Port: $ISIC6202_HTTP_PORT (default 8080). Auth: $ISIC6202_API_TOKEN
  ;; MUST be set (fail-closed — see docs/api.md).
  :serve {:main-opts ["-m" "svcdesk.http"]}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
